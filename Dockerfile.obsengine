FROM python:3.11-slim
WORKDIR /app
COPY obsengine/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY obsengine/ /app/
ENV PYTHONPATH=/app
EXPOSE 8080
ENTRYPOINT ["gunicorn"]
CMD ["--bind", "0.0.0.0:8080", "--workers", "4", "app:app"]
